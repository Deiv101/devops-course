for i in [a, e, i, o, u, u, o, i, e, a]:
 print(count(1)def greet(name):
    """
    This function greets to
    the person passed in as
    a parameter
    """
    print("Hello, " + name + ". Good morning!")

greet('Deiv')

############################################################
for x in ['a', 'e', 'i', 'o', 'u', 'u', 'o', 'i', 'e', 'a']:
 print((x), "\t")
 
